server:
  port: 8080

spring:
  datasource:
    url: jdbc:clickhouse://localhost:8123/rt_music?compress=0
    username: default
    password:
    driver-class-name: com.clickhouse.jdbc.ClickHouseDriver

ai:
  assistant:
    kafka-bootstrap-servers: localhost:9092
    ods-topic: ods_log_ai_assistant
